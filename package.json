{
  "name": "rss-proxy-server",
  "version": "1.0.0",
  "description": "A self-hosted RSS proxy that fetches, caches, and serves multiple RSS feeds.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "pm2": "pm2 start server.js --name rss-proxy && pm2 save && pm2 startup"
  },
  "dependencies": {
    "cron": "^4.1.0",
    "express": "^4.21.2",
    "fs-extra": "^11.3.0",
    "node-fetch": "^3.3.2",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "keywords": [
    "RSS",
    "proxy",
    "cache",
    "torrent",
    "server",
    "feed"
  ],
  "author": "Your Name",
  "license": "MIT"
}